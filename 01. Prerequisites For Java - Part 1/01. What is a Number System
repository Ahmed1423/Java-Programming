Number System:

- A Number System is a structured way to represent numbers using digits (or) symbols.
  
- It allows us to perform mathematical operations such as addition, subtraction, multiplication and division etc.
  
- Every Number System has a different base, that determines how many unique digits it uses. 